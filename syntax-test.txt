type Vec2 = {
    f32,
	f32,
};

type MoreStuff = {
    x: {},
	y: { f32 },
	z: { x: f32, y: f32 },
};

// {} is a zst type. Notice that it could be both a struct
// and a tuple

// A named collection:
// { x: f32, y: f32 }
//
// An unnamed collection:
// { f32, f32 }

type SubCollections = {
    pos: { x: f32, y: f32 },
	vel: { x: f32, y: f32 },
};

// Function pointer: {} -> {}

// Vec is a different type
// than Vec2.
type Vec = Vec2;

const MORE_CONSTANTS = (
	1;
	2;
	3;
	4;
	5
);

const HI2 = (
    1 / 2 / 3 / 4;

    type Hello = { f32 };
	const Other = ( "Hello" );

    let x = 3;
	let y = 50;
	let z = 50;

	50 + Other + 20;

	(
	    3
	)
);
