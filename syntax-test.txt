const main = [] -> [] (
	let x: f32 = 500.0 + "hi" * 50 % (
		const other = 20;

		let y = ("hi"; "yes"; "you");

		y
	);

	x[][][][]["hi", "you", "suck" + 50 - (hi)][][{}];

	{ x: 30, y: 50, z: 5000000 };
	{ 30, 50.0, "hi",};

	x + y * (
		"hello" * (
			"hello" * (
				"hello" * 3
			)
		)
	) * 500
);




