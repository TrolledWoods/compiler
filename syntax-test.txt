type Vec2 = {
    x: f32,
	y: f32,
};

type Substructs = {
    pos: { x: f32, y: f32 },
	vel: { x: f32, y: f32 },
};

// Vec is a different type
// than Vec2.
type Vec = Vec2;

const HI = 3;
const HI2 = 3 * 7 + 3 * 5 ^ 1;
